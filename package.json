{
  "name": "squarebidness-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"no dev server (static site)\"",
    "build": "echo \"no build step (static site)\"",
    "check:assets": "node tools/assets-check.mjs"
  }
}
